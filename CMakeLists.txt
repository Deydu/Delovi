cmake_minimum_required(VERSION 3.4)

project(DELOVI VERSION 1.1.2 LANGUAGES CXX)

add_subdirectory(src)

#if(WIN32)
#	set(CMAKE_INSTALL_PREFIX "D:/Tools/")
#else(WIN32)
#	set(CMAKE_INSTALL_PREFIX $ENV{HOME}/bin/)
#endif(WIN32)
